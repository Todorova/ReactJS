import React from 'react';

let Header = (props) => {

    return (
        <div class="menu">
            <a href="#">Home</a>
            <a href="#">Discover</a>
            <a href="#">Me</a>
            <a href="#">Logout</a>
        </div>
    )

}

export default Header;